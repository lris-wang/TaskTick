[tool.ruff]
target-version = "py312"
line-length = 100
src = ["app", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# FastAPI 依赖注入在参数默认值中使用 Depends()/Query() 是框架约定
ignore = ["B008"]

[tool.ruff.lint.per-file-ignores]
"scripts/**/*.py" = ["E402"]
